// Tinylytics embed code for trellis.tylerbutler.com. Both entry points — the
// landing page layout and the Starlight head override — read it from here so
// the site has exactly one place to turn analytics on.
//
// An empty string disables analytics — neither layout emits a script tag —
// so the site still builds if the embed is ever pulled.
export const TINYLYTICS_EMBED_CODE: string = "qjChxLm4t7pDGebTWRXf";
