[workspace]
members = ["packages/*", "examples"]
ignore-release = ["examples"]

[tasks.hello]
command = "echo hello-from-task"

[publish]
tag-format = "{name}-v{version}"
retry = { attempts = 2, initial-delay = "10ms", multiplier = 2 }
