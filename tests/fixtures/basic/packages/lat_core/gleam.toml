name = "lat_core"
version = "1.2.0"
gleam = ">= 1.0.0"
licences = ["MIT"]
repository = { type = "github", user = "example", repo = "lattice" }

[dependencies]
gleam_stdlib = ">= 0.34.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.2.0 and < 2.0.0"
